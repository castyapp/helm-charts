apiVersion: v1
kind: Service
metadata:
  namespace: casty-api
  name: api-service
  labels:
    app: api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: api
  ports:
    - name: api
      protocol: TCP
      port: {{ .Values.service.apiPort }}
      targetPort: 9002
