apiVersion: v1
kind: Service
metadata:
  namespace: casty-cdn
  name: cdn-service
  labels:
    app: cdn-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: cdn-service
  ports:
    - name: cdn
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 5555